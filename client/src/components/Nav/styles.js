import styled from 'styled-components';

export const NavBlock = styled.div`
  width: 100%;
  display: flex;
`;

export const UploadButtonLayout = styled.div`
  position: absolute;
  right: 0;
`;
